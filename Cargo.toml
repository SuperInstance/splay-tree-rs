[package]
name = "splay-tree-rs"
version = "0.1.0"
edition = "2021"
description = "Splay tree with zig/zig-zig/zig-zag operations, amortized O(log n) access, split/merge, sequential access optimization"
license = "MIT"
repository = "https://github.com/SuperInstance/splay-tree-rs"
keywords = ["splay", "tree", "data-structure", "bst", "amortized"]
categories = ["data-structures"]
